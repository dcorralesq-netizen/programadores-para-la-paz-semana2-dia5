Al enviar una petición POST a http://localhost:3000/registro con un JSON, 
el servidor responde con un objeto que confirma la recepción de los datos. 
Esto demuestra que req.body está capturando correctamente la información 
enviada por el cliente.
